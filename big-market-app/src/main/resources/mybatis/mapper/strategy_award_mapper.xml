<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.learn.infrastructure.persistent.dao.IStrategyAwardDao">

    <resultMap id="dataMap" type="com.learn.infrastructure.persistent.po.StrategyAward">
        <id column="id" property="id"/>
        <result column="strategy_id" property="strategyId"/>
        <result column="award_id" property="awardId"/>
        <result column="award_title" property="awardTitle"/>
        <result column="award_subtitle" property="awardSubTitle"/>
        <result column="award_count" property="awardCount"/>
        <result column="award_count_surplus" property="awardCountSurplus"/>
        <result column="rule_models" property="ruleModels"/>
        <result column="award_rate" property="awardRate"/>
        <result column="sort" property="sort"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>

    </resultMap>

    <select id="queryStrategyAwardList" resultMap="dataMap">
        SELECT strategy_id, award_id, award_title, award_subtitle, award_count, award_count_surplus, rule_models, award_rate, sort
        from strategy_award
        limit 10
    </select>
    
    <select id="queryStrategyAwardListByStrategyId" resultMap="dataMap" parameterType="java.lang.Long">
        select strategy_id, award_id, strategy_award.award_count, strategy_award.award_count_surplus, strategy_award.award_rate
        from strategy_award
        where strategy_id = #{strategy_id}
    </select>
    
    <select id="queryStrategyAwardRuleModels" parameterType="com.learn.infrastructure.persistent.po.StrategyAward" resultType="java.lang.String">
        select strategy_award.rule_models
        from strategy_award
        where strategy_id = #{strategyId} and award_id = #{awardId}
    </select>

</mapper>
